# FE Slip Gaji

This project was generated manually because `npm` and `node` were not detected in the environment.

## Getting Started

1.  Keep sure you have Node.js installed.
2.  Run `npm install` to install dependencies.
3.  Run `npm run dev` to start the development server.

## Stack
- Next.js 14 (App Router)
- Tailwind CSS
- Lucide React (Icons)
